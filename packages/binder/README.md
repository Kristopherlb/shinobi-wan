# @shinobi/binder

Edge compiler: deterministically maps binding/trigger edges to backend-neutral intents (IAM, network, config) with least-privilege defaults (KL-005).

Part of the [Shinobi V3 monorepo](../../README.md) — see the repo root for architecture, invariants, and development commands.

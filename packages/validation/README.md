# @shinobi/validation

Validation pipeline (schema → semantic → determinism) with structured, stable-path errors (KL-006). Validates graph snapshots and intents.

Part of the [Shinobi V3 monorepo](../../README.md) — see the repo root for architecture, invariants, and development commands.

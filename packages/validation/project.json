{
    "name": "validation",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/validation/src",
    "projectType": "library",
    "targets": {
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": [
                "{options.outputFile}"
            ]
        },
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "vitest run --config packages/validation/vitest.config.ts",
                "cwd": "{workspaceRoot}"
            }
        }
    },
    "tags": [
        "scope:validation",
        "type:lib"
    ]
}

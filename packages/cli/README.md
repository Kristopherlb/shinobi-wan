# @shinobi/cli

User-facing CLI (`shinobi`): validate, plan, and up (Pulumi preview/deploy) for Shinobi manifests, plus the MCP integration wrapper. validate/plan never load provider SDKs.

Part of the [Shinobi V3 monorepo](../../README.md) — see the repo root for architecture, invariants, and development commands.

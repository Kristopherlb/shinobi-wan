{
    "name": "adapter-aws",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/adapters/aws/src",
    "projectType": "library",
    "targets": {
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": [
                "{options.outputFile}"
            ]
        },
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "vitest run --config packages/adapters/aws/vitest.config.ts",
                "cwd": "{workspaceRoot}"
            }
        }
    },
    "tags": [
        "scope:adapter",
        "type:lib"
    ]
}
